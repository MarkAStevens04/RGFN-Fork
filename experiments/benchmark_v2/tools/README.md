# campaign-specific drivers and verifiers

A `manifest.py` spanning BOTH pipelines belongs here — resolving a cell from `grid.csv` +
`matrix16/targets.py` + live filesystem status, the way `matrix16/manifest.py` does for one
pipeline. Deliberately not written yet: a bad duplicate is worse than none.
